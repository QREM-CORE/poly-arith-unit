name: PR Build Pipeline

on:
  push:
    branches:
      - main
  pull_request:

permissions:
  pull-requests: write
  contents: read

jobs:
  call-central-builder:
    uses: QREM-CORE/build-tools/.github/workflows/central-builder.yml@ci_upgrade_synth_metrics
    with:
      # Automatically targets the repository and specific commit of the PR
      rtl_repo: ${{ github.repository }}
      commit_sha: ${{ github.sha }}
      build_tools_ref: 'ci_upgrade_synth_metrics'

      # Your comma-separated top modules for the Yosys synthesis matrix
      top_modules: 'poly_arith_unit'

      enable_synth: true
      enable_lint: true
      enable_verilator: true
      enable_vsim: true
